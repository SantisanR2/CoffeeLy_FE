import { FilterByFincaPipe } from './filter-by-finca.pipe';

describe('FilterByFincaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByFincaPipe();
    expect(pipe).toBeTruthy();
  });
});
